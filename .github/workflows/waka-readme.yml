    name: Waka Readme

    on:
      workflow_dispatch:
      schedule:
        # Runs every 2 hours (adjust cron as needed)
        - cron: "0 */2 * * *"

    jobs:
      update-readme:
        name: Update this repo's README
        runs-on: ubuntu-latest
        steps:
          - uses: athul/waka-readme@master
            with:
              WAKATIME_API_KEY: waka_23721be1-9bd1-40bc-a454-2d92a7ff32a1
